# MindIsle 水球静止视觉 v1 修正记录

- 路线：固定正面视角的参考图驱动 2.5D 合成；不使用 Blender 旧渲染作为正向参考。
- 锁定效果：沿用当前 v6 已确认的静止合成视觉。
- 图层整理：只保留正式合成所需的背景、外壳、青绿色体积、边界、两层流层、细水墨雾纹、两个独立点位和曲率高光。
- 点位：冷色点和暖色点分别拆成紧边界 `core` 与独立 `glow`，core 不包含大片周围背景。
- 清理：移除旧的重复点位/高光命名，曲率高光统一为 `11_curvature_highlights.png`。
- 范围：仅静止视觉资产；moving、App、Unity 均未制作或接入。
