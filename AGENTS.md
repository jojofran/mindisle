# MindIsle 产品与开发协作指南

## 文档定位

本文件只描述 MindIsle 的产品目标、用户体验、状态契约、行为准则和验收原则。它不规定具体编程语言、渲染引擎、建模工具、文件路径或资产制作流程。技术方案应根据当前产品目标和验证结果单独决定，不能因为历史实现存在就默认延续。

## 产品目标

MindIsle（心屿）是一款以专注、冥想和音乐为核心的移动 App。用户通过一段安静、可持续的专注时间，逐步改变自己的虚拟世界。

当前产品优先级：

1. 专注 / 冥想主流程；
2. 定时器、音乐和提醒体验；
3. 专注界面的动画、触感和状态反馈；
4. 虚拟世界与可放置内容；
5. 积分、兑换、社交等辅助系统。

在核心专注循环稳定以前，不扩展岛屿、商店或社交功能来掩盖核心体验问题。

## 核心体验

- 用户无需阅读复杂说明，就能理解如何开始、继续、暂停或结束一段专注；
- 中央状态球是专注界面的主要操作核心，也是状态反馈的主要载体；
- 时间负责告知进度，状态球负责表达状态；两者保持清晰的视觉分离；
- 界面保持安静、轻盈、现代和有留白，不用按钮、菜单或说明文字堆出“完整感”；
- 视觉反馈应让用户感到稳定、柔和、可持续，而不是刺激、喧闹或带有游戏化压力；
- 音乐、计时、视觉和触感必须共同服务于专注，不得互相争夺注意力。

## 状态契约

当前水球原型的事实来源是状态，而不是动画或按钮文案。

允许的核心状态：

- `still`：静止状态；
- `transitioning`：从静止进入运动的过渡状态；
- `moving`：运动状态。

当前阶段只承诺：

```text
still → transitioning → moving
reset → still
```

运动状态是否可以再次触摸回到静止状态，必须单独裁决并写入测试；在形成明确契约前，不得由按钮行为、动画表现或文字暗示这一点。

状态是唯一权威，动画只是状态的表现。计时、音乐、视觉、触感、可交互性和调试信息都必须由同一套状态驱动，不能播放与真实状态无关的假动画。

静止状态下，主要运动和形变应冻结，只允许明确约定的极轻微呼吸微动。运动状态应有缓慢、连续、可感知但不打扰专注的变化。

状态球的具体视觉语言、静止/运动状态差异和视觉禁用项见 [`docs/product/state-ball-visual-spec.md`](docs/product/state-ball-visual-spec.md)。该文档描述产品体验，不绑定任何渲染工具或资产制作方式。

## 参考图与设计判断

用户在当前任务中明确指定的参考图优先于历史截图、旧原型和代理自行生成的素材。参考图中直接可见的内容与产品约束发生冲突时，必须记录冲突并遵循当前产品约束，不得默默改变状态定义。

单张参考图无法确定的内容必须标记为推断。代理不得把推断内容伪装成参考事实，也不得为了填补未知部分擅自增加复杂系统。

设计判断应优先回答：用户看到什么、听到什么、触摸什么、状态如何变化，以及这一变化是否让专注更容易。完成感来自体验证据，不来自实现复杂度。

## 开发与迭代原则

- 先明确体验目标，再选择技术实现；
- 每次迭代只推进一个可观察行为；
- 先做最小可体验路径，再扩展系统；
- 允许代理在约束内提出技术方案，但不得把历史工具或目录自动当成产品决策；
- 当结果偏离参考或体验目标时，应回到目标和证据重新判断，而不是继续堆叠实现细节；
- 优先选择可替换、可验证、适合目标设备的最小方案；
- 不为了展示技术能力引入实时物理、复杂 3D、粒子、后处理或其他高成本系统；
- 性能问题先测量再优化，不能用编辑器或单张截图推断真实设备表现。

## 验证规则

每个核心改动至少验证一条当前已经裁决的真实操作路径。当前必测路径是：

```text
进入专注 → 触摸状态球 → transitioning → moving
```

当前最小测试场景还应覆盖：启动静止、读取过渡进度、运动状态持续变化、外部误触不改变状态、确定性重置、挂起时钟冻结、恢复后只继续一次。

验证不能只依赖截图，同时检查：

- 状态是否真实改变；
- 视觉是否与状态一致；
- 计时是否正确推进、冻结和恢复；
- 音乐是否正确开始、淡入、暂停、恢复和停止；
- 触摸区域是否与视觉主体一致；
- 快速重复触摸、切后台和重新进入是否出现错误状态；
- 目标设备上的可读性、流畅度和功耗是否满足要求。

截图用于检查视觉层次，状态读取、日志或断言用于检查隐藏行为。发现问题时遵循：

```text
复现 → 查看视觉和状态证据 → 定位 → 修改 → 重新运行
```

历史截图、旧日志和绿色测试只能作为历史证据，不能单独证明当前实现仍然通过。任何视觉完成声明都必须同时附带真实状态读取和真实输入测试结果。

## 完成标准

当前原型阶段只承诺：

- `still → transitioning → moving` 可以稳定发生；
- 状态、计时和视觉表现保持一致；
- 静止与运动状态在视觉上可区分；
- 中央状态球可以被准确触摸；
- 重置、挂起和恢复行为可重复验证；
- 参考图方向已经转化为可检查的视觉目标。

长期产品完成标准是：用户无需复杂说明，就能通过中央状态球开始、进入静止状态、继续并结束一段专注；音乐和计时在整个过程中可靠；视觉、触感和状态反馈共同形成安静、连续、可理解的体验。

## AI 协作边界

AI 应根据任务读取所需文档，而不是每次修改都遍历整个项目：

- 状态球视觉任务读取 `docs/product/state-ball-visual-spec.md`；
- 运行时或渲染适配任务读取 `docs/architecture/state-ball-app-baseline-v0.1.md`；
- 其他任务只读取与当前变更直接相关的文档。

开始修改时确认目标、范围和完成条件；完成后报告修改内容、实际验证、视觉或状态证据以及未验证事项。若项目已有不接触生产资源的本地验证命令，可以直接运行、修复由本次改动造成的失败并重新验证。

AI 不得用“看起来像”替代真实验证，不得把工具选择当成产品决策，不得在没有证据时宣称完成。遇到参考图歧义、状态契约冲突或验证无法进行时，应明确指出问题并请求裁决，而不是无限猜测或扩大范围。

本文件是产品与协作边界，不是技术架构文档。技术方案、路线选择、实验记录和资产规范应放在独立文档中，并明确其适用范围和有效期。
